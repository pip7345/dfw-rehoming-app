<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Error - DFW Dog Rehoming</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link href="/tailwind.css" rel="stylesheet" />
  <%- include('partials/analytics-umami') %>
  
</head>
<body class="bg-bg-primary text-text-primary">
  <div class="min-h-screen flex items-center justify-center p-6 text-center">
    <div class="bg-bg-secondary rounded-xl p-10 max-w-sm border border-border">
      <div class="text-6xl mb-4">🐕</div>
      <h1 class="text-2xl font-semibold mb-2">Oops! Something went wrong</h1>
      <p class="text-text-secondary mb-6"><%= typeof message !== 'undefined' ? message : 'An unexpected error occurred' %></p>
      <a href="/" class="text-accent hover:underline">← Back to home</a>
    </div>
  </div>
</body>
</html>
