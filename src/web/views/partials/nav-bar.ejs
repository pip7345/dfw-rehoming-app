<!-- Navigation -->
<nav class="nav-bar">
  <a href="/" class="nav-logo flex items-center gap-2 text-4xl font-bold text-white">
    <img src="/logo.png" alt="DFW Rehoming" style="height: 40px;" />
    DFW Rehoming
  </a>
  <div class="flex items-center gap-4">
    <% if (typeof user !== 'undefined' && user) { %>
      <a href="/dashboard" class="btn btn-secondary">Dashboard</a>
      <form method="POST" action="/logout" class="inline">
        <button type="submit" class="nav-link bg-transparent border-none p-0">Logout</button>
      </form>
    <% } else { %>
      <a href="/login" class="btn btn-secondary">List My Pet</a>
    <% } %>
  </div>
</nav>
