<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Register - DFW Dog Rehoming</title>
  <link href="/tailwind.css" rel="stylesheet" />
    <%- include('partials/analytics-umami') %>
</head>
<body class="auth-page">
  <div class="auth-container">
    <!-- Beta Warning Banner -->
    <div class="bg-warning/20 border-2 border-warning rounded-lg p-4 mb-6 text-center">
      <div class="text-warning text-xl font-bold mb-2">⚠️ BETA VERSION - Not Fully Functional</div>
      <p class="text-text-secondary text-sm mb-2">
        We're currently in beta testing. The dashboard and some features are still under development.
      </p>
      <p class="text-text-primary text-sm font-medium">
        Join our <a href="https://t.me/+6ExIKWKCPc9kNTQx" target="_blank" class="text-accent hover:underline font-semibold">Telegram channel</a> to stay updated on when the Dashboard will be fully functional!
      </p>
    </div>
    
    <div class="auth-back-link">
      <a href="/">← Back to home</a>
    </div>
    <div class="auth-logo">🐕</div>
    <h1 class="auth-title">Create Account</h1>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="auth-error"><%= error %></div>
    <% } %>
    <form method="post" action="/register" class="auth-form">
      <div class="form-group">
        <label>Display name</label>
        <input type="text" name="display_name" placeholder="Your name" required />
      </div>
      <div class="form-group">
        <label>Email</label>
        <input type="email" name="email" placeholder="you@example.com" required />
      </div>
      <div class="form-group">
        <label>Password</label>
        <input type="password" name="password" placeholder="••••••••" required />
      </div>
      <button type="submit" class="btn btn-success w-full">Create Account</button>
    </form>
    <p class="auth-footer"><a href="/login">Back to login</a></p>
  </div>
</body>
</html>
